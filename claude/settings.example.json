{
  "//": "Starting point for ~/.claude/settings.json. NOT symlinked: Claude Code rewrites this file at runtime (a /model switch strips the model pin), so each machine owns its own copy and install.sh only seeds it when absent. Change a setting via /config or by editing ~/.claude/settings.json; update this example only when you want the default to change for future machines.",
  "permissions": {
    "defaultMode": "auto"
  },
  "model": "opus[1m]",
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  },
  "effortLevel": "xhigh",
  "modelSettings": {
    "claude-opus-5": {
      "effortLevel": "xhigh"
    }
  },
  "tui": "fullscreen",
  "skipAutoPermissionPrompt": true
}
